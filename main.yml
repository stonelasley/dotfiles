---
- hosts: localhost
  roles:
    - { role: packages , tags: packages }
    - { role: dotfiles , tags: dotfiles }
    - { role: shell, tags: shell}
    - { role: vim, tags: vim }
    - { role: node, tags: node }
